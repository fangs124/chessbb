[package]
name = "chessbb"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
opt-level = 3

[profile.release]
opt-level = 3
panic = "abort"
strip = "symbols"

[lints.clippy]
needless_return = "allow"
implicit_return = "warn"
zero_prefixed_literal = "allow"
